[tool.poetry]
name = "optirocket"
version = "0.1.0"
description = ""
authors = ["Max Perraudin <max.perraudin@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.19.4"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
